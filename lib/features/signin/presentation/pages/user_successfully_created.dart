import 'package:flutter/material.dart';

class UserSuccessfullyCreated extends StatelessWidget{
  const UserSuccessfullyCreated({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:Text('Account successfully created'),
    );
  }

}